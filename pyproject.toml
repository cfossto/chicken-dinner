[project]
name = "chicken-dinner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cfossto", email = "70951673+cfossto@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic>=2.13.5",
]

[project.scripts]
chicken-dinner = "chicken_dinner:main"

[build-system]
requires = ["uv_build>=0.8.12,<0.9.0"]
build-backend = "uv_build"
