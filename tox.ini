[tox]
env_list =
    py310
    py312
    py313
minversion = 4.32.0

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
setenv =
    RESULTS_CSV_PATH = {toxinidir}/results.csv
deps =
    pytest>=6
commands =
    pytest {tty:--color=yes} {posargs}
